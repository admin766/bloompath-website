{
  "name": "bloompath-website",
  "version": "1.0.0",
  "private": true,
  "description": "Bloompath Behavioral Services website with backend form submission.",
  "scripts": {
    "start": "node server.js",
    "dev": "FORM_DRY_RUN=true node server.js",
    "check": "npm audit --omit=dev"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.3"
  }
}
